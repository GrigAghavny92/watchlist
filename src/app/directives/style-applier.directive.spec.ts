import { StyleApplierDirective } from './style-applier.directive';

describe('StyleApplierDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleApplierDirective();
    expect(directive).toBeTruthy();
  });
});
